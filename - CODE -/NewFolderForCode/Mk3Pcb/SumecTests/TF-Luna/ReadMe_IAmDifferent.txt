For this lib's test, see it's function directory
(it's the .ino file. it needs a small change to work (just add a printf and delay to main loop for read, or uncomment the code for set or reset))

is the name a portal ref.? who knows